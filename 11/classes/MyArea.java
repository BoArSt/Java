package classes;
import static java.lang.Math.*;

public class MyArea {
    public static double AreaOfCircle(double r,double area){
        System.out.println("The area of the circle:");
        return area = pow(r,2) * PI;
    }
}
